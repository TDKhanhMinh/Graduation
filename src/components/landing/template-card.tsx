import type { PosterTemplate } from "@/features/posters/schema"

const categoryLabels: Record<string, string> = { wedding: "Đám cưới", birthday: "Sinh nhật", graduation: "Tốt nghiệp", corporate: "Doanh nghiệp", general: "Tối giản" }

export function TemplateCard({ template, onPreview }: { template: PosterTemplate; onPreview: (template: PosterTemplate, trigger: HTMLButtonElement) => void }) {
  const palette = template.palette
  return <article className="group rounded-2xl border bg-card p-3 shadow-sm transition-shadow hover:shadow-md"><div className="relative aspect-[4/5] overflow-hidden rounded-xl p-4 text-white" style={{ background: `linear-gradient(145deg, ${palette[0]}, ${palette[1]})` }}><p className="text-[9px] uppercase tracking-[0.2em] text-white/70">{template.name}</p><p className="absolute bottom-4 left-4 max-w-[75%] font-heading text-2xl font-bold">Linh &amp; Quân</p><div className="absolute right-3 top-3 size-9 rounded bg-white/90" aria-hidden="true" /></div><div className="space-y-2 p-2"><div className="flex items-center justify-between gap-2"><h3 className="min-w-0 truncate font-semibold">{template.name}</h3><span className="shrink-0 text-xs text-muted-foreground">{categoryLabels[template.categories[0]]}</span></div><div className="flex items-center gap-1.5" aria-label={`Palette của ${template.name}`}>{palette.slice(0, 4).map((color) => <span key={color} className="size-4 rounded-full border border-border" style={{ backgroundColor: color }} />)}</div><button type="button" onClick={(event) => onPreview(template, event.currentTarget)} className="mt-1 inline-flex min-h-11 w-full items-center justify-center rounded-lg bg-primary px-3 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/80 focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-focus/50">Xem preview</button></div></article>
}
